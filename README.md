Upload index.html, hero.png and .nojekyll to the root of your GitHub Pages repository. Enable GitHub Pages for the main branch root. Online booking and the admin app currently run on the PureShine hosted backend; those links will open its chatgpt.site address. A custom domain for the booking backend or migration to another host is required to remove that address everywhere.
